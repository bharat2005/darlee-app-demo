import { View, Text } from 'react-native'
import React from 'react'

const Interest = ({width}) => {
  return (
    <View style={{flex:1, width, backgroundColor:'blue'}}>
    
    </View>
  )
}

export default Interest